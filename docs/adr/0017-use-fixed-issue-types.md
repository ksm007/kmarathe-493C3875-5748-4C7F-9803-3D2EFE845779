# 0017: Use fixed issue types

The v1 product supports fixed issue types: task, bug, story, and epic. Custom issue types are deferred because fixed types are enough for sprint planning, filtering, reporting, icons, and AI summaries without adding workflow configuration complexity.
