# 0013: Configure environment-specific values only

Environment-specific values such as URLs, CORS origins, email sender details, OAuth client IDs, provider API keys, secrets, and deploy paths belong in runtime configuration. Stable product concepts such as roles, permissions, task statuses, default priority, and sprint states remain typed product code, not runtime configuration.
