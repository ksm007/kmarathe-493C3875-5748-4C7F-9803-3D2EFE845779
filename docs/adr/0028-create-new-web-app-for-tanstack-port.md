# 0028: Create new web app for TanStack port

The React/TanStack Start dashboard will live in a new Nx app, `apps/web`. The existing Angular app remains in `apps/dashboard` as the behavior and UI reference until the React app reaches launch parity, but new frontend product work should target `apps/web`.
