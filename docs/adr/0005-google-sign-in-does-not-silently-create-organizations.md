# 0005: Google sign-in does not silently create organizations

Google sign-in proves user identity, but it does not silently create a tenant. After Google sign-in, users with memberships enter or choose an organization, users with pending invitations can accept them, and users with neither must explicitly create a new organization through the signup flow.
